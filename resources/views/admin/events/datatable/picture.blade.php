<a href="{{ asset($picture) }}" data-fancybox="gallery" data-caption="{{ __('Ảnh sự kiện: ' . $title) }}">
    <img src="{{ asset($picture) }}" class="rounded mx-auto d-block" width="100%" alt="{{ __('Ảnh sự kiện') }}" />
</a>
